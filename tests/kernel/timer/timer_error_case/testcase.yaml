tests:
  kernel.timer:
    tags: kernel timer userspace ignore_faults
  kernel.timer.linker_generator:
    arch_allow: arm
    tags: kernel timer userspace ignore_faults linker_generator
    extra_configs:
      - CONFIG_CMAKE_LINKER_GENERATOR=y
