tests:
  kernel.fifo.timeout:
    tags: kernel
  kernel.fifo.timeout.linker_generator:
    arch_allow: arm
    tags: kernel linker_generator
    extra_configs:
      - CONFIG_CMAKE_LINKER_GENERATOR=y
