tests:
  kernel.threads.init:
    tags: kernel threads userspace
  kernel.threads.init.linker_generator:
    arch_allow: arm
    tags: kernel threads userspace linker_generator
    extra_configs:
      - CONFIG_CMAKE_LINKER_GENERATOR=y
